/**
 * Authentication Configuration
 * Control whether login is enabled by modifying ENABLE_AUTH
 * true: Enable login (login required to access)
 * false: Disable login (no login required to access)
 */
export const AUTH_CONFIG = {
  // Login feature toggle, set to false to temporarily disable login
  ENABLE_AUTH: false
}
